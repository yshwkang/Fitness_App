# PRD: 운동 코치 앱 (Fitness Coach App) 프로토타입

## 1. 개요

손그림 와이어프레임(`fitness_UI.png`)을 기반으로, 모바일 운동 코치 앱의 **실행 가능한 클릭형 프로토타입**을 제작한다. 실제 서비스 백엔드 없이도 화면 간 이동과 핵심 인터랙션(세트 기록, 운동 시작/완료 등)을 체험할 수 있는 수준의 완성도를 목표로 한다.

- **원본 자료**: `fitness_UI.png` (손그림 와이어프레임, 4화면 + 공통 규칙 메모)
- **산출물**: 모바일 뷰포트(약 375~430px 폭) 기준으로 렌더링되는 웹 기반 인터랙티브 프로토타입 1식
- **화면 수**: 4개 (오늘의 운동 → 운동 기록 → 운동 통계 → 운동 완료)

## 2. 목표

- 와이어프레임의 화면 구조·정보 배치·핵심 기능을 그대로 유지하면서, 실제 피트니스 앱 수준의 시각적 완성도로 재해석한다.
- 4개 화면이 하나의 디자인 시스템(색상, 타이포그래피, 아이콘, 버튼/카드/차트 스타일, 여백, 톤)으로 일관되게 연결되도록 한다.
- 하단 탭 내비게이션과 화면 전환이 실제로 동작하는 클릭 가능한 프로토타입으로 제작해, 데스크톱 브라우저에서도 모바일 앱을 쓰는 듯한 경험을 제공한다.
- 모든 UI 텍스트는 한국어를 유지한다.

## 3. 타겟 사용자

- 개인 운동(웨이트 트레이닝) 기록을 스마트폰으로 관리하고 싶은 일반 사용자
- "오늘 뭘 할지 → 어떻게 기록할지 → 얼마나 늘었는지 → 오늘 어땠는지"를 하나의 루틴으로 확인하고 싶은 사용자

## 4. 정보 구조 (IA) 및 공통 규칙

와이어프레임 하단의 "공통" 메모를 그대로 따른다.

- **상단 바**: 좌측 메뉴 아이콘(≡) 또는 뒤로가기(<), 우측 알림 아이콘(🔔) — 화면 성격에 따라 선택적으로 배치
- **하단 탭 내비게이션** (4개 화면 공통 고정): 홈(오늘의 운동) / 기록 / 통계 / 마이
  - "운동 완료" 화면은 플로우상 별도 진입점(운동 기록 화면에서 "운동 완료" 버튼)이므로 하단 탭 노출 여부는 디자인 단계에서 판단
- **화면 전환 플로우**:
  1. 오늘의 운동 (홈) → [운동 시작] → 운동 기록
  2. 운동 기록 → [운동 완료] → 운동 완료 화면
  3. 운동 완료 → [기록 저장] → 오늘의 운동(홈)으로 복귀
  4. 하단 탭으로 오늘의 운동 / 운동 기록 / 운동 통계 자유 이동 가능
- 전체 톤앤매너, 색상, 스타일, 아이콘, 차트 형태는 자유롭게 판단하되 4화면 전반에 통일되게 적용한다.

## 5. 화면별 요구사항

### 5-1. 오늘의 운동 (홈)

**목적**: 오늘 수행할 운동을 확인하고 시작하는 진입 화면

- 상단: 메뉴 아이콘(≡), 알림 아이콘(🔔)
- 인사 문구: "안녕하세요! 오늘도 화이팅 💪" 형태의 사용자 환영 메시지
- **오늘의 운동 카드**
  - 운동 부위명 (예: 등·이두)
  - 예상 소요 시간 (예: 50분)
  - 종목 수 (예: 5종목)
  - [운동 시작] 버튼 → 운동 기록 화면으로 이동
- **이번 주 운동 현황**
  - 월~일 7일 각각을 점(도트)으로 표시, 완료일은 채워진 도트로 구분
  - 진행률 텍스트 (예: "2/3회 완료")
- **최근 기록**
  - 카드형 리스트 (썸네일 + 부위명 + 날짜 + 총 운동 볼륨, 예: "등 운동 8/26 4,820kg")
  - 2개 이상 카드 노출, 가로 스크롤 또는 그리드 레이아웃
- 하단: 탭 내비게이션 (홈/기록/통계/마이, "홈" 활성 상태)

### 5-2. 운동 기록

**목적**: 오늘의 운동을 종목별·세트별로 기록

- 상단: 뒤로가기(<), 타이틀 "오늘의 운동"
- **종목별 세트 기록 카드** (종목마다 반복되는 구조, 와이어프레임 기준 2개 이상: 랫풀다운, 시티드 로우 등)
  - 종목명
  - 세트 테이블: SET / KG / REPS / 완료 체크
  - 완료된 세트는 체크 표시, 미완료는 빈 상태로 구분
  - [+ 세트 추가] 버튼으로 세트 행 추가 가능
- 하단 고정 버튼: [운동 완료] → 운동 완료 화면으로 이동
- 하단 탭 내비게이션 유지 ("기록" 활성 상태)

### 5-3. 운동 통계

**목적**: 주간 운동 현황과 누적 데이터를 시각적으로 확인

- 상단: 메뉴 아이콘(≡), 타이틀 "통계"
- **이번 주 요약**: "2/3회" 형태의 진행 텍스트 + 월~일 요일별 완료 도트
- **총 운동 볼륨 차트**: 주차별(1주/2주/3주 …) 추이를 보여주는 라인 차트
- **부위별 비율**: 도넛/파이 차트 + 범례 (부위명 + 퍼센트, 예: 등 35%, 하체 30%, 가슴 20%, 기타 15%)
- 하단: 탭 내비게이션 ("통계" 활성 상태)

### 5-4. 운동 완료

**목적**: 운동 종료 후 요약을 확인하고 컨디션·메모를 기록해 저장

- 상단: 완료를 상징하는 체크 아이콘(원형 배지)
- "운동 완료!" 타이틀 + 총 운동 시간 (예: "52분 운동")
- **요약 카드**: 총 운동량(kg) 강조 표시
- **상세 지표 리스트**: 운동 종목 수 / 완료 세트 수 / 예상 소모 칼로리
- **컨디션 체크**: "오늘 컨디션은 어땠나요?" + 5단계 이모지 선택(매우 나쁨~매우 좋음)
- **운동 메모**: 자유 텍스트 입력 필드
- [기록 저장] 버튼 → 저장 후 오늘의 운동(홈) 화면으로 복귀
- 이 화면은 플로우의 종료 지점이므로 하단 탭 내비게이션 노출은 선택 사항으로 판단(요약 확인에 집중하는 풀스크린 형태 고려 가능)

## 6. 핵심 기능 (와이어프레임 "+핵심 기능" 메모 기준)

1. 오늘의 운동 확인 및 시작
2. 세트 기록 (무게 kg, 횟수 reps 입력·수정, 완료 체크 토글)
3. 운동 통계 및 변화 확인 (주간 진행률, 볼륨 추이, 부위별 비율)
4. 운동 완료 후 요약 확인 및 기록 저장 (컨디션, 메모 포함)

## 7. 인터랙션 / 프로토타입 동작 범위

- 하단 탭 4개 및 화면 내 버튼(운동 시작 / 운동 완료 / 기록 저장 / 뒤로가기)을 통한 실제 화면 전환
- 세트 테이블의 완료 체크 토글, [+ 세트 추가] 클릭 시 새 세트 행 추가
- 운동 완료 화면의 이모지 컨디션 선택(단일 선택), 메모 입력
- 화면 간 이동 시 애니메이션/트랜지션으로 실제 앱과 유사한 전환감 제공
- 데이터는 목업(mock) 데이터로 구성하며, 세트 추가·체크 등 사용자 조작은 화면 내 상태로 반영(별도 서버 저장 없음)

## 8. 디자인 시스템 방향

색상, 타이포그래피, 아이콘, 버튼/카드/차트 스타일, 여백, 전체 분위기는 자유 판단이나, 아래 원칙을 따른다.

- 실제 피트니스 앱(예: 나이키 트레이닝 클럽, 캐치핏 등) 수준의 직관성과 완성도
- 하나의 컬러 팔레트(주 색상 1~2개 + 중립색 + 상태색)를 4화면에 일관 적용
- 카드, 버튼, 입력 필드, 차트(라인/도넛) 등 반복 요소는 공통 컴포넌트 스타일로 통일
- 운동/에너지를 연상시키는 톤(활동적이고 동기부여가 되는 분위기)이되 가독성과 정보 위계를 우선
- 모바일 UI 컨벤션(안전영역 여백, 터치 타깃 크기, 하단 탭 고정 등) 준수

## 9. 범위 외 (Out of Scope)

- 실제 회원가입/로그인, 서버 API 연동, 데이터 영속 저장
- 운동 종목 추가/검색, 루틴 생성·편집 등 와이어프레임에 없는 기능
- "마이" 탭 및 메뉴(≡)/알림(🔔) 진입 시 상세 화면 (아이콘/진입점만 배치, 상세 화면은 4개 화면 범위 밖)

## 10. 기술 스택

실제 iOS/Android 모바일 앱으로 빌드 가능해야 하므로, **React Native (Expo, TypeScript)** 기반 크로스플랫폼 스택을 채택한다.

- **프레임워크**: React Native + Expo (managed workflow), TypeScript
- **네비게이션**: `@react-navigation/native` + `bottom-tabs` — 홈/기록/통계/마이 하단 탭, 운동 기록→운동 완료는 스택 네비게이션으로 전환
- **스타일링**: `NativeWind`(Tailwind 문법) 또는 `StyleSheet` 기반 공통 테마 토큰(색상/타이포/간격)으로 4화면 일관성 확보
- **차트**: `react-native-svg` 기반 커스텀 컴포넌트로 라인 차트(주간 볼륨)·도넛 차트(부위별 비율) 직접 구현, 또는 `victory-native` 활용
- **아이콘**: `@expo/vector-icons` (또는 인라인 SVG 커스텀 아이콘)
- **폰트**: `expo-google-fonts`의 Noto Sans KR로 한글 가독성 확보
- **상태 관리**: 화면 간 공유 상태(오늘의 운동, 세트 기록, 통계)는 React Context 또는 Zustand로 관리, 로컬 영속화는 `AsyncStorage` 사용 — 서버/백엔드 연동 없음(mock 데이터 기반)
- **개발/미리보기**: Expo Go 앱으로 QR 스캔 후 실기기에서 즉시 확인 (Xcode/Android Studio 네이티브 빌드 없이도 미리보기 가능), 필요 시 `expo run:android`/`eas build`로 네이티브 빌드

**선택 이유**: 하나의 코드베이스로 iOS·Android를 모두 지원하며, Windows 개발 환경에서도 Expo Go를 통해 별도 네이티브 툴체인 없이 즉시 실기기 미리보기가 가능함. JS/TS 생태계라 컴포넌트 재사용과 반복 작업이 빠르고, 이후 실제 서비스(백엔드 연동, 인증 등)로 확장할 때도 자연스럽게 이어갈 수 있음.

**대안**: `Flutter(Dart)`도 동일하게 단일 코드베이스로 크로스플랫폼을 지원하며 커스텀 UI/픽셀 단위 제어와 `fl_chart` 등 차트 생태계가 강점이나, 팀이 Dart에 익숙하지 않다면 React Native 대비 진입 장벽이 있음. UI 커스터마이징 자유도와 네이티브에 가까운 성능이 최우선이라면 Flutter를 검토할 수 있음.

## 11. 성공 기준

- 와이어프레임에 명시된 4화면의 정보 요소가 누락 없이 반영됨
- 4화면이 동일한 디자인 시스템으로 통일감 있게 보임
- 브라우저에서 클릭만으로 오늘의 운동 → 운동 기록 → 운동 완료 → 홈 복귀, 그리고 통계 탭 이동까지 전체 플로우를 끊김 없이 체험 가능
- 텍스트는 전부 한국어로 표기됨
